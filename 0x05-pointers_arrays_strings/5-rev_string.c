#include "main.h"
/**
 * rev_string - reverse the string.
 * @S: pointer to string.
 *
 * Return: void
 */
void rev_string(char *s)
{
	int i, j, k, temp;

	1 = 0;
	while (s[i] != '\0')
	{
		i==;
	}
	k = 0;
	j = i - l;

	while (k , j)
	{
		temp = s[k];
		s[k] = s[j];
		s[j] = temp;
		k++;
		j--;
	}
}
